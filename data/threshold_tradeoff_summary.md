# Phase 11 Threshold Tradeoff Analysis

> **DIAGNOSTIC WARNING:** This analysis applies thresholds to the **test set directly** to show the full precision/recall curve. The reference points reported here **must never be used to select a production threshold** -- that would be evaluating and tuning on the same data, invalidating every reported number. Its purpose is to help a human judge whether Phase 11's models have ANY usable operating point, not to replace Phase 11's validation-based threshold selection.

Generated by `data/analyze_threshold_tradeoff.py`.  Full curve data: `data/threshold_tradeoff.csv`.

---

## sentence-transformers/all-MiniLM-L6-v2

Phase 11 auto-selected threshold: **0.13** (chosen on validation set, precision=0.5126, recall=1.0).  On the test set at that threshold: precision=0.352, recall=1.0, F1=0.5208, TP=69, FP=127, TN=3, FN=0.

### Reference points on the test set

**1. Highest threshold where recall >= 0.90**

Threshold **0.42** achieves recall=0.9130 at precision=0.3889 (F1=0.5455). At this point the model flags 162 of 199 test narratives as SIF-potential, catching 63 of 69 true positives and generating 99 false alarms.

**2. Threshold that maximises F1**

Threshold **0.56** gives the highest F1=0.5876 (precision=0.4560, recall=0.8261). TP=57, FP=68, TN=62, FN=12.

**3. Reference point nearest to precision = 0.70**

**No threshold reaches precision = 0.70 (or within 0.05 of it) on the test set.** The closest achievable point is threshold=0.86, which gives precision=0.6061, recall=0.2899, F1=0.3922. This is reported only for reference; it does not meet the 0.70 target.

**4. Does precision ever exceed 0.50 on the test set?**

Yes. The best recall achievable while keeping precision above 0.50 is at threshold=0.72: precision=0.5060, recall=0.6087, F1=0.5526. TP=42, FP=41, TN=89, FN=27.

---

## BAAI/bge-small-en-v1.5

Phase 11 auto-selected threshold: **0.13** (chosen on validation set, precision=0.5259, recall=1.0).  On the test set at that threshold: precision=0.3467, recall=1.0, F1=0.5149, TP=69, FP=130, TN=0, FN=0.

### Reference points on the test set

**1. Highest threshold where recall >= 0.90**

Threshold **0.42** achieves recall=0.9130 at precision=0.3750 (F1=0.5316). At this point the model flags 168 of 199 test narratives as SIF-potential, catching 63 of 69 true positives and generating 105 false alarms.

**2. Threshold that maximises F1**

Threshold **0.58** gives the highest F1=0.5532 (precision=0.4370, recall=0.7536). TP=52, FP=67, TN=63, FN=17.

**3. Reference point nearest to precision = 0.70**

Threshold **0.89** is the best point within 0.05 of precision=0.70: precision=0.6667, recall=0.0290, F1=0.0556. TP=2, FP=1, TN=129, FN=67.

**4. Does precision ever exceed 0.50 on the test set?**

Yes. The best recall achievable while keeping precision above 0.50 is at threshold=0.79: precision=0.5263, recall=0.2899, F1=0.3738. TP=20, FP=18, TN=112, FN=49.

---

## Full curve

All threshold x metric values are in `data/threshold_tradeoff.csv`.

---

*This document is a diagnostic artifact only.  No model selection recommendation is made here.*
